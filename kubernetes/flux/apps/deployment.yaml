apiVersion: apps/v1
kind: Deployment
metadata:
  name: teste
  namespace: default
spec:
  replicas: 4
  selector:
    matchLabels:
      app: teste
  template:
    metadata:
      labels:
        app: teste
    spec:
      containers:
      - image: nginx:alpine
        name: nginx